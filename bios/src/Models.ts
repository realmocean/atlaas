
export namespace Models {
    export interface Organization {
        $id: string;
        name: string;
    }
    export interface Project {
        $id: string;
        name: string;
    }

    export interface Form {
        $id: string;
        name: string;
    }
}